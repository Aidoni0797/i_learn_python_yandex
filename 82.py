# Настройте функцию так, чтобы она не сломалась при вызове без аргументов
def lets_go():
    name = 'Друг'
    target = 'Python'
    print(name + ', пойдём учить ' + target)


# Вызовите функцию lets_go без аргументов
lets_go()